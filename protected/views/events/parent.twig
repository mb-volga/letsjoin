{% extends 'layouts/content.twig' %}
{% block content %}
    {%  for item in items %}
        <div class="block">
            {{ CHtml.link(item.title, '#')|raw }}
        </div>
    {% endfor %}
{% endblock content %}