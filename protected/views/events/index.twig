{% extends 'layouts/content.twig' %}

{% block content %}
    {% for item in items %}
        <div class="block">
            {#{{ CHtml.link(item.title, 'events/'~item.id~'/'~item.id)|raw }}#}
            {{ item.id }}
        </div>
    {% endfor %}
{% endblock %}